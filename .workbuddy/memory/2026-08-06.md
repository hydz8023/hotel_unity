# 2026-08-06

- 从游戏设计视角分析了 `doc/idear.md`（武侠客栈经营游戏策划案）。
- 核心结论：真正差异化钩子是"网格布置直接影响经营结果（布置即策略）"，但文档列了 7 个核心卖点导致重点分散；范围爆炸（14 系统 × 4 阶段，24 周不现实）是头号风险。
- 设计亮点：门口小乞丐软引导（教学藏进 flavor）、人岗兜底（缺岗玩家手动顶）、难度多维而非单纯数值、网格布置已建好（v0.1）。
- 主要风险：实时员工 AI / NavMesh 最贵却排最后才做；前 30 分钟全手动反向节奏（最累在最前、最爽在后期）；经济后期通胀（进钱源 > 固定支出 sink）。
- 建议：以"布置即策略"为唯一主轴；先做能玩竖切片；把"布置有用"提前到前 10 分钟；纸上先算经济 sources/sinks 找通胀拐点。
- 假设单局 ~30-40 分钟、偏慢节奏；若做碎片手游需重写核心循环。
- 用户确认核心应定为"经营客栈循环"（扩建/招待/经营/赚钱），认可把原 7 卖点收敛为 1 主轴。设计主次：核心=经营循环，网格布置=做取舍的舞台，武侠=皮，势力/暗战/菜谱=后期加料。关键风险仍是后期通胀与前期全手动疲劳；循环好玩与否取决于"每个动作有无代价（空间/钱/需求冲突/时间稀缺）"。
- 已动手改写 `doc/idear.md` 第 1.3 节：删除原 7 条核心卖点，替换为「一句话核心 + 三条不可妥协设计标准（经营循环优先 / 布置即决策 / 稀缺才有意思）」+ 题材皮与后期加料的定位说明。后续所有系统评审都应先过这三条。
- 用户计划先做验证玩法的简单 demo。建议 demo 只验证三条标准：1 个网格房间(复用 v0.1 摆放/存档) + 3 种需求冲突客人(普通/文人/侠客) + 手动经营循环(接待→满足→收钱→日结) + 4-5 件有用家具 + 极简银两进出 + 客人耐心/有限空间。明确砍掉：员工AI/NavMesh、势力、暗战、菜谱研发、风闻、多房型、难度滑块、节日。须先定义失败信号(A布置无后果/B不稀缺/C循环不清/D反向不靠布置)再开做。
- 用户问网格 vs 自由摆放。结论：对本游戏(核心=经营循环+布置即决策)网格更好——格满即满让稀缺/冲突清晰、标签与动线易算、技术已建(v0.1)；自由摆放会藏起取舍且重写碰撞/寻路。采用网格逻辑+视觉微偏移的混合补观感，不改底层网格。
- 已将"网格优先+视觉微偏移"写入 `doc/idear.md` 4.4.0 节：网格为底层逻辑不作自由摆放，理由=布置即决策/稀缺才有意思/技术已建(v0.1)；观感用旋转贴边补偿且不影响占格；设红线阻止改自由摆放的提议。
- 已新建 `doc/demo_scope.md`：汇总 demo 玩法验证范围——只验三条设计标准；必做内容(1网格房复用v0.1+3冲突客人+手动循环+4-5家具+极简经济+耐心/有限空间+清晰反馈)；砍掉员工AI/势力/暗战/菜谱/风闻/多房型/难度/节日；失败信号A布置无后果/B不稀缺/C循环不清/D反向；起步数值全[PLACEHOLDER]；建议纸上先跑。
- 已新建 `doc/demo_tuning.csv`：demo 数值初稿(tuning spreadsheet)。含客人(3种×房费/耐心/需求)、家具(床/屏风/兵器架/灶台/桌椅×占格/价格/效果)、标签达成、需求对照、经济(起始银两150/房费10/餐饮8/扩建80)、空间节奏(8×8/5-8分钟/8-12人)。所有非基准值标[PLACEHOLDER]待playtest。
- 从程序视角审计了 `Assets/Script/` 现有代码结构并产出 `doc/architecture-analysis.md`。结论：分层清晰(SO纯数据/单一职责/输入闸门解耦)。P0 正确性 Bug：①家具旋转 90° 后 `FurniturePlacer.RotateFurniture` 不交换 gridSize，非正方形家具占用 footprint 错误(应交换 x/y 并重吸附)；②`GridSystem.occupiedGrid` 仅运行时内存且 Start 前为 null(潜在 NRE)。P1 耦合反模式：`PlacementPreview` 用 `FindObjectOfType` 取 GridSystem/FurniturePlacer；玩法层(`FurniturePlacer`/`GameInputGate`)直接依赖 `UIManager.Instance` 单例(建议改 SO 事件通道 + InputModeVariable)。P2：每次放置全量重存档、运行时实例三份冗余表示(建议 RuntimeSet<SO>)、`PlacedFurniture.instanceId` 死字段。P3：目录大小写 Assets/Script vs Assets/script 不一致、中文枚举 FurnitureCategory、InnLayoutData.version 未做迁移。
- 已为 `doc/demo_scope.md` 追加第 8 节"程序实施方案(架构映射)"：把 demo 玩法(1网格房+3冲突客人+手动循环+5家具+极简经济+耐心/有限空间)落到现有 `Assets/Script/` 架构。复用 v0.1 摆放/存档；新增 FurnitureTag 枚举、FurnitureData.providesTags、GuestData(SO)、NeedMatcher(纯逻辑可单测)、Guest/InnDirector(MonoBehaviour)、FloatVariable(SO, SilverVariable 银两真源不持久化)、InnConfig(SO 集中数值)；修改 FurnitureData/FurniturePlacer/PlacementPreview/GridSystem/HUDPanel。前置修复 P0(旋转 footprint + NRE)与 P1(PlacementPreview 注入引用)。实现分 Phase0-4，每阶段带可验证里程碑；开放问题：静修"距大堂"用 entryCell 曼哈顿距离、床并发上限、扩建最简实现、单例暂不解耦。
